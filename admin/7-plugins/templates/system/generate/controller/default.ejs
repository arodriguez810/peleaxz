            $scope.$watch('<%= scope %>.<%= field %>', function (value) {
                var rules = [];
                //rules here
                //rules.push(VALIDATION.general.required(value));
                //rules.push(VALIDATION.general.maliciousCode(value));
                VALIDATION.validate(<%= scope %>, "<%= field %>", rules);
            });