#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../phantom-html2pdf/bin/phantom-html2pdf" "$@"
  ret=$?
else 
  node  "$basedir/../phantom-html2pdf/bin/phantom-html2pdf" "$@"
  ret=$?
fi
exit $ret
