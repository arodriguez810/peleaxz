<td ng-repeat="(key, value) in <%= scope %>.columns() track by key"
    ng-show="value.visible!=false"
    class="{{::value.class || ''}} context-control"
    style="{{::value.style || ''}}"
    data-column="{{::key}}"
    ng-click="<%= scope %>.cell.select($event,row)"
    ng-dblclick="<%= scope %>.cell.dblselect($event)"
    oncontextmenu="return false;"
>

    <span
            ng-show="<%= scope %>.rowDeleted(row)===''"
            ng-bind-html="<%= scope %>.drawCheck(key,row)"
            ng-click="<%= scope %>.check(row)"
    >
    </span>
    <span ng-bind-html="<%= scope %>.cellValue(key,value,row)"
          ng-click="<%= scope %>.cell.click(key,value,row)"
          ng-dblclick="<%= scope %>.cell.dblclick(key,value,row)"
          ng-mousedown="<%= scope %>.cell.mousedown(key,value,row)"
          ng-mouseenter="<%= scope %>.cell.mouseenter(key,value,row)"
          ng-mouseleave="<%= scope %>.cell.mouseleave(key,value,row)"
          ng-mousemove="<%= scope %>.cell.mousemove(key,value,row)"
          ng-mouseover="<%= scope %>.cell.mouseover(key,value,row)"
          ng-mouseup="<%= scope %>.cell.mouseup(key,value,row)"
    ></span>
    <!--<div class="text-center dragon-actions dragon-cell-action" style="display: none"-->
         <!--ng-show="(<%= scope %>.allow('actions') && <%= scope %>.allow(['edit','remove','active']))">-->
        <!--<ul class="icons-list" style="z-index: 99999" ng-show="<%= scope %>.rowDeleted(row)===''">-->
            <!--<li ng-repeat="(min, option) in <%= scope %>.getOptions(false) track by min"-->
                <!--ng-show="<%= scope %>.allow(<%= scope %>.option.permission(option,row)) && <%= scope %>.option.show(option,row)">-->
                <!--<a-->
                        <!--data-action="{{<%= scope %>.option.action(option,row)}}"-->
                        <!--title="{{<%= scope %>.option.title(option,row)}}"-->
                        <!--ng-click="<%= scope %>.option.click(option,row,false)" class="dropdown-toggle"-->
                        <!--data-toggle="dropdown">-->
                    <!--<i class=" icon-{{<%= scope %>.option.icon(option,row)}}"></i>-->
                <!--</a>-->
                <!--<ul class="dropdown-menu dropdown-menu-right dragon-menu" ng-show="option.menus">-->
                    <!--<li ng-repeat="(item, menu) in option.menus track by item"-->
                        <!--ng-show="<%= scope %>.allow(<%= scope %>.option.permission(menu,row)) && <%= scope %>.option.show(menu,row)">-->
                        <!--<a-->
                                <!--title="{{<%= scope %>.option.text(menu,row)}}"-->
                                <!--data-action="{{<%= scope %>.option.action(menu,row)}}"-->
                                <!--ng-click="<%= scope %>.option.click(menu,row)">-->
                            <!--<i class="icon-{{<%= scope %>.option.icon(menu,row)}}"></i>-->
                            <!--{{<%= scope %>.option.text(menu,row)}}-->
                        <!--</a>-->
                    <!--</li>-->
                <!--</ul>-->
            <!--</li>-->
        <!--</ul>-->
    <!--</div>-->
</td>